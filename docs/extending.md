# 확장하기 — 규칙 추가 · 새 언어 갈래 추가

이 문서는 **손을 대는 곳을 전부 나열**한다. 배경(왜 이런 구조인지)은 [architecture.md](architecture.md),
빌드/테스트/인코딩 규약은 [../CONTRIBUTING.md](../CONTRIBUTING.md).

두 가지 시나리오를 다룬다.

| | 시나리오 | 난이도 | 손대는 곳 |
|---|---|---|---|
| **레시피 1** | [기존 C# 갈래에 규칙 추가](#레시피-1-기존-c-갈래에-규칙-추가) | 낮음 | 엔진 파일 1개 + 3줄 배선 + GUI 체크박스 + 픽스처 |
| **레시피 2** | [새 언어 갈래 추가 (C/C++ 예시)](#레시피-2-새-언어-갈래-추가-cc-예시) | 중간 | 새 엔진 + 새 러너(**`.cs` 하드코딩 4곳 교체**) + GUI 배선 11접점 |

---

## 레시피 1: 기존 C# 갈래에 규칙 추가

[코드 규칙] 갈래(엔진 `SparrowSyntaxFix`) 기준으로 설명한다. [주석·레이아웃] 갈래(엔진 `SparrowCommentFix`)도 구조는 같지만
규칙 구현이 `Program.cs` 안의 rewrite 메서드 + 규칙 키 레지스트리 형태다.

### 설계 원칙 (코드보다 먼저)

- **정형 패턴만.** 반복 발생하고, 구문만 보고 판정 가능한 것에 한정한다. 일반 목적 리팩터링 도구가 아니다.
- **의미 분석이 필요하면 하지 말거나 갈라라.** [코드 규칙]·[주석·레이아웃]은 심볼을 모른다([architecture.md 3.1](architecture.md#31-핵심-성질-프로젝트를-로드하지도-컴파일하지도-않는다)).
  안전한 부분과 애매한 부분이 섞이면 `-safe` / `-narrowing` 두 규칙으로 나눈다
  (`objectvar-safe`/`objectvar-narrowing`, `arrayvar-safe`/`arrayvar-narrowing` 가 그 예다).
- **애매하면 skip.** 잘못 고치는 것보다 안 고치는 게 낫다. skip 조건을 문서에 명시한다.
- **문자열/정규식으로 코드를 편집하지 않는다.** 반드시 Roslyn 노드 위에서 편집한다
  (그래야 문자열 리터럴 안의 `//`, `&&` 같은 게 절대 안 다친다).
- **멱등해야 한다.** 두 번 돌려도 두 번째엔 아무 변화가 없어야 한다.
- **위험한 규칙은 `review-needed`.** 커밋 라벨에 `검토필요` 가 드러나게 하고, 기본 규칙 집합에서 뺀다(opt-in).
- **잔여 위험은 솔직히 적는다.** 엔진 README 에 "이건 못 막는다"를 쓴다(예: named type enum 은 `foreachcast` 에서 구별 불가).

### 접점 ([코드 규칙] 규칙 하나 = 아래 전부)

`hoge` 라는 새 규칙을 넣는다고 하자.

#### ① 엔진: rewriter 파일 하나

`tools/_internal/SparrowSyntaxFix/HogeRewriter.cs` — 규칙 하나 = 파일 하나가 이 레포의 관례지만,
**파일 수와 규칙 키 수는 1:1 이 아니다.** 숫자를 정확히 해 두면 헷갈릴 일이 없다.

| 세는 대상 | 개수 | 비고 |
|---|---|---|
| 활성 rewriter 파일 (`*Rewriter.cs`) | **12** | `ArrayVar`, `EmptyStmt`, `FieldSplit`, `ForHoist`, `ForVar`, `ForeachCast`, `LocalConst`, `NullVar`, `ObjectInitializer`, `ObjectVar`, `ObviousVar`, `Parens` |
| `SyntaxRule` 플래그 / CLI 규칙 키 (`Program.cs` 의 `RuleOrder`) | **14** | `ObjectVar` 와 `ArrayVar` 는 **한 파일이 `-safe`/`-narrowing` 두 키**를 담당한다 |
| 그 밖의 CLI 입력 토큰 | — | `all`(전체) · `nullcast`(`nullvar` 의 legacy alias). **둘 다 규칙이 아니다** — `RuleOrder` 에 없고 카운트 맵에도 안 나온다. 러너도 alias 를 `$canonicalRules` 가 아니라 `$ruleAliases` 로 따로 두고 canonical 로 접는다 |

`RewriteEngine.cs` 의 `RewriteResult.NullCastEdits` 도 `NullVarEdits` 로 넘겨주는 **옛 픽스처 문구용 back-compat 프로퍼티**일 뿐,
별도 규칙이 아니다. 새 규칙을 셀 때 이것들을 규칙으로 착각하지 말 것.

```csharp
internal sealed class HogeRewriter : CSharpSyntaxRewriter
{
    public int Count { get; private set; }

    public override SyntaxNode? VisitLocalDeclarationStatement(LocalDeclarationStatementSyntax node)
    {
        // 1) 가드를 먼저 전부 통과시킨다 (통과 못 하면 base 를 그대로 반환 = 무변경)
        if (!VarRewriteHelpers.IsSingleLocal(node, out var decl, out var declarator))
            return base.VisitLocalDeclarationStatement(node);
        // ... 규칙별 skip 조건들

        // 2) trivia 를 보존하며 노드를 갈아끼운다
        Count++;
        return node.WithDeclaration(newDecl);
    }
}
```

공유 인프라를 쓴다(직접 만들지 말 것):

| 파일 | 제공하는 것 |
|---|---|
| `VarRewriteHelpers.cs` | `IsSingleLocal`(using local/이미 var/다중 declarator 걸러냄), `IsVarType`, `WithVarType`, `CastValue`, `TypeEquals`, `IsPredefinedValueType`, 리터럴 판별 등 |
| `RewriteEngine.cs` | 규칙 적용 루프 + 규칙별 카운트 맵 + `ToFullString()` 비교로 "정말 바뀌었나" 판정 |
| `FileDiscovery.cs` | 대상 `.cs` 열거 + 생성/백업 파일 제외(`*.g.cs`, `*.Designer.cs`, `AssemblyInfo.cs`, `<auto-generated` 헤더 등) + `--files-from` 목록 파싱 |
| `SourceFileIo.cs` | 인코딩/BOM/개행 보존 읽기, 비-UTF8 skip, 원자적 쓰기 |

#### ② 엔진: `SyntaxRule` 플래그 추가

`tools/_internal/SparrowSyntaxFix/RewriteEngine.cs`

```csharp
internal enum SyntaxRule
{
    ...
    ForHoist = 1 << 13,
    Hoge     = 1 << 14,          // ← 추가
    Default  = Parens | ObjectVarSafe | ObviousVar | ArrayVarSafe,     // 안전 규칙만. 보통 여긴 안 건드린다
    All      = ... | ForHoist | Hoge,                                  // ← All 에도 추가
}
```

같은 파일의 `RewriteEngine.Rewrite()` 에 적용 블록을, `NewCounts()` 에 카운트 키를 추가한다.
`RewriteResult` 에 `HogeEdits` 프로퍼티를 더하면 인프로세스 픽스처가 읽기 쉬워진다.

#### ③ 엔진: `Program.cs` 배선 3곳

`tools/_internal/SparrowSyntaxFix/Program.cs`

1. `RuleOrder` 배열에 `("hoge", SyntaxRule.Hoge)` — 출력 순서와 카운트 맵의 원천이다.
2. `TryParseRules()` 의 switch 에 `case "hoge": rules |= SyntaxRule.Hoge; break;`
3. `Usage()` 의 규칙 목록 문자열에 `hoge` 추가(사용자가 보는 도움말).

#### ④ 러너: 규칙 라벨 + (opt-in 이면) 대화형 질문

`tools/_internal/SparrowSyntaxFix/Run-SparrowSyntaxFix.ps1`

1. `$labels` 해시테이블에 `hoge = '... (SparrowSyntaxFix)'` — 규칙별 커밋 메시지가 된다.
   위험 규칙이면 앞에 `검토필요:` 를 붙인다.
2. `$canonicalRules` 배열에 `'hoge'` 추가 — **안 하면 러너가 "지원하지 않는 규칙" 으로 거부한다.**
3. opt-in 규칙이면 `$optionalRules` 에 Y/N 프롬프트 항목을 추가한다.
4. 기본 안전 규칙이면 `param()` 의 `[string[]]$Rules = @(...)` 기본값에도 넣는다.

> `tools/Run-SparrowAll.ps1` 의 `$SyntaxRules` 기본 문자열도 함께 볼 것 — 원큐 실행에서 켜고 싶다면 여기에도 추가한다.

#### ⑤ GUI: 체크박스 + 설명 + 수집

`tools/SparrowRunner.Gui/`

1. `MainWindow.xaml` 의 `CodeRuleTab` 안 `UniformGrid` 에 체크박스 하나:
   `<CheckBox x:Name="ASHoge" Content="사람이 읽는 규칙 이름" Style="{StaticResource RuleCheck}"/>`
   (기본 켜짐이면 `IsChecked="True"`. `[검토필요]` 규칙은 Content 앞에 `[검토필요] ` 를 붙이는 관례.)
2. `MainWindow.xaml.cs` 의 `BuildJobs()` → `CollectRules(...)` 목록에 `(ASHoge, "hoge")` 추가.
   **이걸 빠뜨리면 체크박스는 보이는데 아무 일도 일어나지 않는다.**
3. `MainWindow.xaml.cs` 의 `InitializeRuleInfo()` 에 `AddRuleInfo(ASHoge, 제목, 설명, 근거, 예제)` 추가.
   이름과 달리 **단순 설명 등록이 아니다** — `AddRuleInfo` 는 툴팁·설명 패널과 함께
   그 체크박스의 **`Checked`/`Unchecked` 이벤트까지 등록**한다. 빠뜨리면 설명 패널이 비는 데서 그치지 않고
   **체크를 켜고 꺼도 요약바가 아예 갱신되지 않는다.**
4. **요약바 카운트 두 곳.** 둘 다 `CollectRules` 와 **따로** 관리되므로 함께 고쳐야 한다.
   - `MainWindow.xaml.cs` `UpdateSummary()` 의 `int count = CountChecked(ASObjectVarSafe, …)` 목록에 `ASHoge` 추가
     — 요약바의 `코드 규칙 · 선택 N개`.
   - `review-needed` 규칙이라면 `ReviewNeededCodeRules` 배열([주석·레이아웃] 면 `ReviewNeededCommentRules`)에도 추가
     — 요약바의 `검토필요 N`.

   > **여기가 이 레시피에서 가장 놓치기 쉬운 곳이다.** 빠뜨리면 규칙은 정상 실행되는데
   > **요약바 숫자만 하나 모자라게** 나온다. 그리고 **이 결함은 어떤 테스트도 잡지 못한다** —
   > UIA 하네스는 탭·라벨·인자 전달을 단정할 뿐 요약바 카운트와 체크박스 집합의 일치를 단정하지 않는다.
   > 눈으로 확인하는 수밖에 없다.
   >
   > `review-needed` 여부는 **네 곳이 반드시 일치**해야 한다: 엔진 `README.md` 규칙 표의 `Commit policy` 열(단일 진실)
   > · 러너 `$labels` 의 `검토필요:` 접두 · `MainWindow.xaml` 체크박스 Content 의 `[검토필요] ` 접두
   > · 위 `ReviewNeededCodeRules`/`ReviewNeededCommentRules` 배열. 어긋나면 "검토필요 커밋만 revert" 작업에서 위험 규칙이 통째로 누락된다.

#### ⑥ 테스트: 픽스처

1. **인프로세스**: `tools/_internal/SparrowSyntaxFix/FixtureTests/Program.cs` 에
   positive / negative / **멱등성** / **문자열 리터럴 안전성** 케이스를 넣는다.
2. **디스크 E2E**: `tests/sparrow-syntaxfix-fixtures.ps1` 에 BOM·CRLF 보존, 생성파일 skip, `--dry-run` 케이스를 넣는다.
3. 교차 규칙 상호작용이 걱정되면 `tests/sparrow-loop-tests.ps1` 에도 추가한다.

#### ⑦ 문서

`tools/_internal/SparrowSyntaxFix/README.md` 의 규칙 표에 한 줄
(변환 예시 + `normal`/`review-needed` 커밋 정책 + **skip 조건**과 **잔여 위험**).

### 레시피 1 체크리스트

- [ ] `<Name>Rewriter.cs` 추가 — 가드 먼저, trivia 보존, `Count` 증가
- [ ] `SyntaxRule` 에 플래그 추가 + `All` 에 포함 (`Default` 는 안전 규칙만)
- [ ] `RewriteEngine.Rewrite()` 적용 블록 + `NewCounts()` 키 (+ `RewriteResult` 프로퍼티)
- [ ] `Program.cs`: `RuleOrder` / `TryParseRules` switch / `Usage()` 도움말
- [ ] 러너: `$labels` / `$canonicalRules` / (opt-in 이면) `$optionalRules` / (기본이면) `$Rules` 기본값
- [ ] GUI: `MainWindow.xaml` 체크박스 / `CollectRules` 쌍 / `AddRuleInfo` / **`UpdateSummary()` 의 `CountChecked` 목록** (+ `review-needed` 면 `ReviewNeededCodeRules`/`ReviewNeededCommentRules`)
- [ ] `review-needed` 표기가 네 곳(엔진 README 표 · 러너 `$labels` · XAML 라벨 · `ReviewNeeded*Rules`)에서 일치
- [ ] 픽스처: positive · negative · **멱등성** · **문자열 리터럴 안전성** · BOM/CRLF 보존
- [ ] 엔진 README 규칙 표 갱신(skip 조건 + 잔여 위험 명시)
- [ ] `./validate.ps1 -All` 통과

---

## 레시피 2: 새 언어 갈래 추가 (C/C++ 예시)

**목표**: [코드 자동수정] 안에 `[C/C++ 코드 규칙]` 같은 탭을 만들어, C/C++ 소스에 대한 결정론적 자동수정을 돌린다.

### 2.0 먼저: [XLS 분리] 는 손댈 필요가 없다

Sparrow 가 검출한 C/C++ 항목을 **체커별 md 로 분리하는 일은 이미 된다.**
익스포터는 xls 의 `언어` 컬럼을 읽지 않고 소스 셀을 파싱 없이 문자열로 옮기며, 경로 매칭도 문자열 비교다
([architecture.md 4.2](architecture.md#42-언어-무관성이-공짜인-이유)).

> **`SparrowXlsExport`/`SparrowXlsExport.Core` 를 건드리지 마라.** 새 언어를 위해 할 일이 없다.
> 여기서 만드는 것은 **자동수정 갈래**뿐이다.

### 2.1 왜 Roslyn 을 못 쓰나

[코드 규칙]·[주석·레이아웃] 의 엔진은 `CSharpSyntaxTree.ParseText` 위에 서 있다. 이건 **C# 파서**다.
C/C++ 소스를 넣으면 파싱이 실패하거나, 더 나쁘게는 엉뚱한 트리를 만든다. 재사용할 수 없다.

그래서 **자체 엔진**이 필요하다. 선택지는 셋이다.

| 선택지 | 장점 | 단점 | 어울리는 규칙 |
|---|---|---|---|
| **libclang** (`clang -fsyntax-only`, Python `clang.cindex`, C# `ClangSharp`) | 진짜 C/C++ AST. 정확도 최고 | 헤더/include 경로/매크로 정의가 필요 = **빌드 환경 의존**. 폐쇄망 반입 부담(LLVM 바이너리) | 타입·스코프를 알아야 하는 규칙 |
| **tree-sitter** (`tree-sitter-c`, `tree-sitter-cpp`) | **파서만** — 컴파일 안 함, include 불필요. 이 레포의 "구문 전용" 철학과 정확히 맞는다. 오류 복원력 좋음 | 의미 정보 없음(Roslyn 구문 전용과 같은 한계). 네이티브 바인딩 반입 필요 | 형식/레이아웃/괄호/선언 분리 등 대부분의 Sparrow 코드 규칙 |
| **줄 단위 / 정규식** | 의존성 0, 폐쇄망 최고 | 문자열·주석 안을 잘못 건드리기 쉽다. **반드시 문자열/주석 스캐너를 직접 구현**해야 한다 | 후행 공백, 탭→스페이스처럼 진짜 지역적인 규칙만 |

**권장**: tree-sitter. 이 레포가 Roslyn 에게 요구하는 것(구문 트리 + trivia 보존 + 컴파일 불필요)과 성질이 같다.
정규식 경로를 고를 거라면 **문자열 리터럴·주석 안은 절대 건드리지 않는다**는 보장을 픽스처로 증명해야 한다
([주석·레이아웃] 의 SAFETY 픽스처가 그 선례다).

무엇을 고르든 **엔진 계약**은 동일하다.

- 입력: 파일 경로들 (또는 `--files-from` 목록) + `--rules <콤마 목록>`
- 출력: 파일을 **제자리에서** 수정 + stdout 에 규칙별 건수 요약
- `--dry-run` 은 아무것도 쓰지 않고 건수만 보고
- 인코딩/BOM/개행 **보존**, 비-UTF8 파일은 skip, **원자적 쓰기**, 멱등
- exit code: `0` 성공(변경 여부 무관) / `1` 실제 오류 / `2` 사용법 오류

### 2.2 핵심: 러너 계약만 지키면 GUI 에 붙는다

GUI 는 엔진을 모른다. **러너 `.ps1` 을 `powershell.exe` 로 실행하고 stdout 을 로그창에 흘릴 뿐이다.**
넘기는 인자는 이게 전부다(`MainWindow.xaml.cs` 의 `BuildJobs()`).

| 인자 | 값 | 새 러너가 해야 할 일 |
|---|---|---|
| `-Solution <경로>` | `.sln`/`.csproj`/폴더 (C/C++ 면 `.vcxproj`/`.sln`/폴더) | **파일이면 `Split-Path -Parent`, 폴더면 그대로** = 소스 루트 |
| `-Rules <a,b,c>` | 콤마 구분 규칙 키 | 규칙마다 엔진을 한 번씩 실행 |
| `-LogDir <경로>` | 폴더 | `Run-<이름>.<stamp>.log` 기록. **폴더를 스스로 만들 것** — 기존 러너들은 안 만들어서 없으면 `[FATAL]` 로 죽는다. GUI 는 여기에 **대상 소스 루트**를 넘긴다([usage.md 진단 로그](usage.md#자동수정-러너-로그는-대상-소스-루트에-쌓인다)) |
| `-FilesFrom <파일>` | CSV/줄 목록 | 그 목록만 대상. **비었거나 소스 루트 밖뿐이면 전체로 확대하지 말고 실패**시킬 것. GUI 는 **항상** 이 인자를 넘긴다 |
| `-Commit` \| `-NoCommit` | 스위치 | `-Commit` 이면 규칙별 커밋, `-NoCommit` 이면 파일만 수정 |

**가장 빠른 길: 기존 러너를 그대로 베낀다.**
`tools/_internal/SparrowSyntaxFix/Run-SparrowSyntaxFix.ps1` 을 복사해
`tools/_internal/SparrowCppFix/Run-SparrowCppFix.ps1` 로 만들고 아래만 바꾼다.

1. `param()` — 위 5개 인자는 그대로 두고, 필요하면 `-ExePath`/`-DryRun`/`-VerifyCmd` 도 그대로 유지.
2. `$labels` / `$canonicalRules` / `$optionalRules` — 새 언어의 규칙 키로 교체.
3. `Resolve-Tool` — 엔진 확보 순서를 새 엔진에 맞춘다.
   `-ExePath` → `publish\<엔진>.exe` → (소스+SDK 있으면) 증분 빌드 → 기존 `bin\Release\...`.
   **.NET 이 아닌 엔진이면 이 함수만 그 기술에 맞게 바꾸면 된다**(예: 반입한 네이티브 exe 경로 확인).
4. **git 커밋/게이트 로직 — 반드시 고쳐야 한다.** 아래 [2.2.1](#221-필수-러너의-cs-하드코딩-4곳) 참조.
   이건 선택이 아니다. **하나(pathspec 생성)는 `-NoCommit` 이어도 러너를 즉사시킨다.**
5. 파일 글롭 확장자 — 이건 **러너에 없다.** 어느 레이어를 고쳐야 하는지는
   아래 [2.2.2](#222-소스-파일-확장자는-러너가-아니라-엔진gui-에-있다) 참조.

#### 2.2.1 (필수) 러너의 `.cs` 하드코딩 4곳

베낀 러너에는 **언어 무관이 아닌 `.cs` 문자열이 4군데** 있다. `Run-SparrowSyntaxFix.ps1` 기준 위치와 증상:

| # | 위치 | 코드 | 안 고치면 |
|---|---|---|---|
| **1** | `New-GitPathspecFile` 안 | `if (-not $full.EndsWith('.cs', ...)) { continue }` | **러너가 즉사한다. 최우선.** 아래 설명 |
| 2 | `Test-GitTargetChanged` 의 `-FilesFrom` 없는 폴백 | `git status --porcelain -- '*.cs'` | C/C++ 변경을 못 보고 **"변경 없음"으로 커밋을 조용히 건너뛴다** |
| 3 | `Restore-GitTargets` 의 폴백 revert | `git checkout -- '*.cs'` | 게이트 실패 시 **C/C++ edits 가 되돌려지지 않는다** — 깨진 코드가 다음 규칙으로 넘어간다 |
| 4 | `Invoke-GitCommitStep` 의 `-FilesFrom` 없는 경로 | `git add -- '*.cs'` | **C/C++ 변경이 커밋에서 통째로 누락된다** |

**왜 1번이 치명적인가** — `New-GitPathspecFile` 호출은 **커밋 여부와 전혀 무관하게** 일어난다:

```powershell
if ($FilesFrom) { $gitPathspecFile = New-GitPathspecFile -Root $root -FilesFromPath $FilesFrom }
if ($FilesFrom -and -not $gitPathspecFile) { throw "-FilesFrom에 소스 루트 아래 .cs 파일이 없습니다: $FilesFrom" }
```

`.cs` 필터가 `-FilesFrom` 목록을 전부 걸러내면 함수가 `$null` 을 돌려주고 **다음 줄이 throw** 한다.
그리고 GUI 는 `-FilesFrom` 을 **언제나** 넘긴다(`BuildJobs()` 의 공통 인자 루프). 즉:

> **`-NoCommit` 으로 실행해도, 커밋 코드에 도달하기 한참 전에 러너가 `[FATAL]` 로 죽는다.**
> "커밋을 안 쓰니 git 로직은 상관없다" 는 성립하지 않는다.

2\~4번은 `-FilesFrom` 이 없을 때만 타는 폴백/revert 경로라 **조용히 틀린다** — 즉사보다 나쁠 수 있다.
확장자 집합을 러너 상단에 상수 하나(`$SourceGlobs = @('*.c','*.cc','*.cpp','*.h','*.hpp')`)로 빼고
네 곳이 모두 그것을 참조하게 만드는 편이 안전하다.

#### 2.2.2 소스 파일 확장자는 러너가 아니라 엔진/GUI 에 있다

**러너에는 소스 파일 글롭이 없다.** 러너는 `-FilesFrom` 이 없을 때 엔진에 `$root`(폴더)만 넘기고,
**열거는 엔진이 한다.** 그래서 "러너의 `*.cs` 를 바꾼다"는 지시대로 러너를 뒤지면 고칠 게 안 나오고,
"이미 언어 무관이구나"로 오독하기 딱 좋다. 실제로 확장자를 정하는 곳은 둘이다.

| 레이어 | 파일 | 무엇 | 새 언어에서 |
|---|---|---|---|
| **엔진** | `tools/_internal/SparrowSyntaxFix/FileDiscovery.cs` | `Directory.EnumerateFiles(resolved, "*.cs", SearchOption.AllDirectories)` + 단일 파일일 때의 `EndsWith(".cs")` 체크 + 생성/백업 제외 규칙 | **새 엔진에 자기 몫으로 새로 쓴다**(기존 파일을 고치는 게 아니다) |
| **GUI** | `tools/SparrowRunner.Gui/SourceScopeDiscovery.cs` | 범위 트리 열거 `directory.EnumerateFiles("*.cs", SearchOption.TopDirectoryOnly)` + 대상이 파일일 때의 `.cs` 확장자 수용 체크 | **여기를 넓혀야** C/C++ 파일이 범위 트리에 뜬다. 이게 `-FilesFrom` manifest 의 원천이다 |

**단, [코드 규칙]·[주석·레이아웃]과 파일 집합이 섞이면 안 된다.** 범위 트리가 `.cs` 와 `.cpp` 를 한 집합으로 모으면
C# 러너에 `.cpp` 가 넘어간다. 갈래별로 확장자 집합을 갈라 주는 편이 안전하다(2.3 의 마지막 불릿과 같은 얘기다).

### 2.3 GUI 배선 — 11개 접점

대부분 `tools/SparrowRunner.Gui/` 안이고, 배포·테스트 쪽 둘(6·7)만 밖이다.
**0 번을 먼저 하지 않으면 나머지 10개를 다 해도 실행 버튼이 안 먹는다.**

| # | 파일 | 무엇 |
|---|---|---|
| **0 · P0** | `MainWindow.xaml.cs` 실행 핸들러의 **범위 가드** | `EnsureScopeAsync(target)` 결과가 `SelectedFiles.Count == 0` 이면 `"선택된 .cs 파일이 없습니다"` 를 띄우고 **`BuildJobs()` 에 도달하기 전에 return** 한다. 범위 트리가 `.cs` 만 모으는 한 **C/C++ 폴더는 여기서 먼저 막힌다** — 새 러너를 아무리 잘 만들어도 호출조차 안 된다. [2.2.2](#222-소스-파일-확장자는-러너가-아니라-엔진gui-에-있다) 의 `SourceScopeDiscovery` 확장과 **한 쌍**이고, 메시지 문구도 `.cs` 고정이라 함께 고친다 |
| 1 | `MainWindow.xaml.cs` (`private enum ActiveMode { CodeRule, Comment, XlsSplit, None }`) | 항목 추가 → `{ CodeRule, Comment, XlsSplit, CppRule, None }` |
| 2 | `MainWindow.xaml.cs` `CurrentMode()` | 새 탭 `TabItem` 을 그 enum 값에 매핑 (`if (ReferenceEquals(selected, CppRuleTab)) return ActiveMode.CppRule;`) |
| 3 | `MainWindow.xaml` | `RulesTabs` 안에 `<TabItem x:Name="CppRuleTab" AutomationProperties.AutomationId="CppRuleTab" Header="C/C++ 코드 규칙">` + 규칙 체크박스들. **AutomationId 를 반드시 줄 것** — UIA 하네스가 그걸로 찾는다 |
| 4 | `MainWindow.xaml.cs` `BuildJobs()` | `runCppRule` 분기 + `CollectRules(...)` + `jobs.Add(new RunnerJob("C/C++ 코드 규칙 수정", Path.Combine(_toolsDir, "_internal", "SparrowCppFix", "Run-SparrowCppFix.ps1"), rules, logDir))`. 공통 인자 루프(`-Solution`/`-Rules`/`-LogDir`/`-FilesFrom`/`-Commit`\|`-NoCommit`)는 이미 모든 job 에 적용되므로 손댈 필요 없다 |
| 5 | `MainWindow.xaml.cs` `UpdateRunButtonForMode()` / `UpdateSummary()` / `RulesTabs_SelectionChanged()` / `SectionTabs_SelectionChanged()` | 새 갈래의 실행 버튼 라벨(`"C/C++ 코드 규칙 수정 실행"`), 요약바 문구, 탭 전환 시 규칙 설명 초기화 분기 |
| 6 | `tools/publish-airgap.ps1` | `$projects` 배열에 새 엔진 추가(현재 4종 → 5종). 반입 체크리스트 출력도 자동으로 늘어난다 |
| 7 | `tests/gui-uia-tests.ps1` | `$SUB_TABS = @('코드 규칙', '주석·레이아웃')` 를 새 탭 포함으로 갱신 + 하위 탭 개수 단정 갱신 |
| **8** | `MainWindow.xaml` 의 **경고 배너** | `SectionFixTab` 바로 안, `Grid.Row="0"` 의 주황 `Border` — **`RulesTabs` 바깥**이다. 즉 새 C/C++ 하위 탭을 골라도 **그대로 떠 있다.** 문구가 *"Roslyn C# 파서로 코드를 재작성하므로 C/C++ 등 다른 언어에는 쓸 수 없습니다. C/C++ 결과는 위의 [XLS 분리] 대분류를 사용하세요."* 라서, 고치지 않으면 **제품이 사용자에게 거짓말을 한다.** 선택한 하위 탭에 따라 문구를 바꾸거나 배너를 `RulesTabs` 안으로 옮긴다 |
| **9** | `MainWindow.xaml.cs` `UpdateSummary()` 의 **`SectionHintText`** | 삼항의 **else 쪽**에 `"코드 자동수정: C# 전용입니다. …"` 가 묻혀 있다. `switch (mode)` 케이스만 추가하고 이 줄을 지나치기 매우 쉽다 — 새 갈래에서도 "C# 전용" 이 그대로 뜬다 |
| **10** | `MainWindow.xaml.cs` `BrowseFileButton_Click` 의 **파일 대화상자 필터** | `Filter = "Solution/Project (*.sln;*.csproj)\|*.sln;*.csproj\|모든 파일 (*.*)\|*.*"` — **`.vcxproj` 를 고를 수 없다.** [2.2 의 인자 표](#22-핵심-러너-계약만-지키면-gui-에-붙는다)는 `.vcxproj` 를 대상으로 명시하므로, 여기를 안 고치면 문서와 UI 가 어긋난다(사용자는 "모든 파일" 로 우회해야 한다) |

여기에 더해:

- **`BuildJobs()` 맨 앞의 조기 반환 가드를 반드시 함께 고칠 것.** 현재는
  `if (!runCodeRule && !runComment) { return jobs; }` 라서, 새 갈래만 켠 상태로 실행하면
  **아무 job 도 안 만들어지고 조용히 끝난다.** 새 플래그를 이 조건에 넣지 않으면
  "실행 버튼을 눌렀는데 아무 일도 안 일어난다" 로 나타난다 — 가장 걸리기 쉬운 함정이다.
- 실행 핸들러에서 `runCodeRule`/`runComment` 를 계산하는 곳(`bool runCodeRule = mode == ActiveMode.CodeRule;` 형태)도
  새 갈래를 알아야 하고, `BuildJobs(...)` 시그니처에 새 플래그 인자를 늘려야 한다.
- `MainWindow.xaml.cs` 의 `InitializeRuleInfo()` 에 새 체크박스들의 `AddRuleInfo(...)` 를 넣어야 설명 패널이 빈칸이 아니다.
  (레시피 1 의 ⑤ 와 같은 이유로 **`UpdateSummary()` 의 `CountChecked` 목록**도 새 갈래 몫을 추가해야 요약바 숫자가 맞는다.)
- **범위 트리 확장자**는 위 표의 **0 번**과 [2.2.2](#222-소스-파일-확장자는-러너가-아니라-엔진gui-에-있다) 에서 다뤘다.
  `SourceScopeDiscovery` 를 넓히는 것과 실행 핸들러 가드를 푸는 것은 **반드시 함께** 해야 하며,
  갈래별로 확장자 집합을 갈라 두지 않으면 C# 러너에 `.cpp` 가 넘어간다.

### 2.4 설계 판단 하나: 대분류 라벨을 어떻게 할 것인가

현재 대분류 라벨은 **`코드 자동수정 (C#)`** 이고, 문서·배너에도 "C# 전용" 이 명시돼 있다.
C/C++ 을 넣으면 그 라벨이 거짓이 된다. 두 선택지가 있고, **어느 쪽도 강요하지 않는다.**

| | (a) 대분류를 언어 중립으로 + 하위 탭으로 언어를 가른다 | (b) 새 대분류를 하나 더 만든다 |
|---|---|---|
| 모습 | `[코드 자동수정]` → 하위 탭 `[C# 코드 규칙] [C# 주석·레이아웃] [C/C++ 코드 규칙]` | `[코드 자동수정 (C#)]` `[코드 자동수정 (C/C++)]` `[XLS 분리 (모든 언어)]` |
| 장점 | 화면 수가 안 늘어난다. 공통 인프라(대상 경로 입력·범위 트리·커밋 체크박스)를 그대로 공유 | 언어별로 화면이 완전히 갈려 **입력이 다른 것**(대상 파일 확장자, 범위 트리 소스)을 자연스럽게 분리. 기존 C# 화면이 전혀 안 흔들림 |
| 단점 | 탭이 늘어나 한 화면에 언어가 섞인다. 범위 트리가 두 언어의 파일을 함께 보여 혼동될 수 있다 | 대분류가 3개가 되어 최상단이 붐빈다. 공통 UI 를 복제하거나 UserControl 로 빼야 한다 |
| 건드리는 곳 | 위 11접점 그대로 | 위 11접점 + `SectionTabs`/`IsXlsSection()` 계열 분기 전반 |

**판단 기준**: 범위 트리와 대상 입력이 C# 과 **같은 소스 트리**를 공유하면 (a), 실제로 다른 트리·다른 입력이면 (b).
어느 쪽이든 **`XLS 분리 (모든 언어)` 라벨은 그대로 둔다** — 이미 참이다.

라벨을 바꾸면 함께 고쳐야 하는 곳: `MainWindow.xaml` 의 `Header`,
`tests/gui-uia-tests.ps1` 의 라벨 단정, `README.md`/`docs/usage.md` 의 화면 구성 표.
**그리고 "C# 전용" 문구가 박혀 있는 두 곳** — 2.3 표의 **8**(경고 배너)과 **9**(`SectionHintText`).
라벨만 중립화하고 이 둘을 놔두면 화면 안에서 서로 모순되는 문구가 동시에 보인다.

### 2.5 최소 검증 경로

아래 순서로 올라가면 실패 지점이 어디인지 항상 명확하다.

1. **엔진 단위 픽스처** — 새 엔진만 단독으로. positive / negative / 멱등성 / **문자열·주석 안 불변** / 인코딩·개행 보존.
   ([코드 규칙] 의 `FixtureTests/`, [주석·레이아웃] 의 `tests/sparrow-commentfix-fixtures.ps1` 이 본보기다.)
2. **러너 dry 실행** — GUI 없이, GUI 가 넘길 인자 그대로.

   > **전제조건 2가지.** 둘 다 안 지키면 러너가 시작하자마자 죽거나 조용히 아무것도 안 한다.
   >
   > 1. **`-LogDir` 폴더가 미리 존재해야 한다.** 기존 러너들은 `-LogDir` 을 만들지 않고
   >    바로 그 안에 로그를 쓰므로, 폴더가 없으면 첫 `Out-File` 이 던지고 `[FATAL]` 로 중단된다.
   >    (새 러너를 만들 때 `New-Item -ItemType Directory -Force` 를 넣어 두면 이 함정이 사라진다.)
   > 2. **`-Rules` 를 반드시 명시한다.** 기존 러너는 `-Rules` 가 없으면 opt-in 규칙마다
   >    `Read-Host` Y/N 프롬프트를 띄운다. 비대화형 stdin 에서는 응답이 전부 빈 문자열이 되어
   >    **opt-in 규칙이 조용히 전부 꺼진 채 "변경 없음" 을 보고**한다 — 통과처럼 보이는 실패다.
   >    자동화/CI 라면 예외 없이 `-Rules` 를 준다.

   ```powershell
   New-Item -ItemType Directory -Force C:\work\logs | Out-Null   # ← 먼저
   .\tools\_internal\SparrowCppFix\Run-SparrowCppFix.ps1 `
       -Solution C:\work\CppProj -Rules parens -LogDir C:\work\logs `
       -FilesFrom C:\work\files.csv -NoCommit
   ```

   `-DryRun` 을 지원한다면 그걸로 먼저. 로그 파일이 `-LogDir` 에 생기는지, 규칙별 건수가 찍히는지 확인.
   **대상은 남의 소스 트리가 아니라 샌드박스여야 한다** — 안전한 시험 대상은
   [usage.md 의 "파괴적 기능을 안전하게 시험하기"](usage.md#파괴적-기능을-안전하게-시험하기-샌드박스) 참조.
3. **GUI 탭 UIA 단정** — `tests/gui-uia-tests.ps1` 에 새 탭이 존재/선택 가능하고, 라벨·순서가 맞고,
   실행 버튼 라벨이 바뀌고, 그 탭에서만 새 러너가 호출되는지를 넣는다.
   `./validate.ps1 -IncludeGuiUiaTests` 로 돌린다(창이 잠깐 뜬다. 데스크톱 세션이 없으면 self-skip).
4. **전체 게이트** — `./validate.ps1 -All`.
5. **(선택) 실제 회귀 증명** — 대상 프로젝트에서 수정 전/후 Sparrow 를 다시 돌리고
   `tools/Compare-Sparrow.ps1 -Before before.xls -After after.xls` 로 G2 게이트를 통과시킨다.
   **이게 진짜 증거다** — 파서 편집이 Sparrow 검출 소멸을 보장하지는 않는다.

### 레시피 2 체크리스트

**엔진**

- [ ] 파서 선택 근거를 README 에 한 문단으로 적었다(왜 Roslyn 이 아닌지 포함)
- [ ] CLI 계약: `<file-or-dir>...` / `--files-from` / `--root` / `--rules` / `--dry-run`, exit `0`/`1`/`2`
- [ ] 인코딩·BOM·개행 보존, 비-UTF8 skip, 원자적 쓰기
- [ ] 멱등 (두 번 돌려도 두 번째엔 무변화)
- [ ] 문자열 리터럴·주석 안을 건드리지 않음을 픽스처로 증명
- [ ] 생성/빌드 산출물 파일 제외 규칙

**러너 (`Run-<이름>.ps1`)**

- [ ] `-Solution` / `-Rules` / `-LogDir` / `-FilesFrom` / `-Commit`\|`-NoCommit` 5개를 받는다
- [ ] `.sln`/프로젝트 파일이면 부모 폴더로 환원, 폴더면 그대로
- [ ] `-FilesFrom` 이 비었거나 루트 밖뿐이면 **전체로 확대하지 않고 실패**
- [ ] 엔진 확보 순서(`-ExePath` → `publish\` → 빌드 → 기존 산출물)와 폐쇄망 폴백
- [ ] 규칙별 실행 + `-LogDir` 에 타임스탬프 로그 (**`-LogDir` 폴더를 스스로 만든다** — 없으면 죽는 함정 제거)
- [ ] **`.cs` 하드코딩 4곳을 새 언어 확장자로 교체**([2.2.1](#221-필수-러너의-cs-하드코딩-4곳)) — pathspec 생성 / `git status --porcelain` / `git checkout` / `git add`
- [ ] `-Commit` 시 규칙별 커밋(선택 파일만: `--only --pathspec-from-file`) + `-VerifyCmd` 게이트

**GUI**

- [ ] **범위 가드 + `SourceScopeDiscovery` 확장자 집합** (2.3 의 **0 번** — 이걸 먼저 하지 않으면 나머지가 무의미)
- [ ] `ActiveMode` enum 항목 추가
- [ ] `CurrentMode()` 매핑
- [ ] `MainWindow.xaml` 새 `TabItem` (+ **AutomationId**) + 규칙 체크박스(+ AutomationId 권장)
- [ ] `BuildJobs()` 에 새 `RunnerJob` (+ 맨 앞 조기 반환 가드에 새 플래그 추가)
- [ ] 실행 버튼 라벨 / 요약바 / 탭 전환 분기 (`UpdateRunButtonForMode`, `UpdateSummary`, `*_SelectionChanged`)
- [ ] `InitializeRuleInfo()` 에 새 규칙 설명 + `UpdateSummary()` 의 `CountChecked` 목록
- [ ] **경고 배너 문구**(2.3 의 8) — 새 탭 위에서 "C/C++ 에는 쓸 수 없습니다" 가 뜨지 않는다
- [ ] **`SectionHintText`**(2.3 의 9) — "코드 자동수정: C# 전용입니다" 가 새 갈래에서 안 뜬다
- [ ] **파일 대화상자 필터**(2.3 의 10) — `.vcxproj` 를 고를 수 있다

**배포·테스트·문서**

- [ ] `tools/publish-airgap.ps1` `$projects` 에 새 엔진 추가
- [ ] `tests/gui-uia-tests.ps1` 의 `$SUB_TABS`/하위 탭 개수 단정 갱신
- [ ] 새 엔진용 픽스처 테스트 + `validate.ps1` 의 소스 존재 목록·테스트 목록에 등록
- [ ] `README.md` 세 갈래 표 + 언어 지원 절 갱신, `docs/usage.md` 화면 구성 표 갱신
- [ ] 새 엔진 폴더에 `README.md`(규칙 표 · CLI · 안전성 보장 · 잔여 위험)
- [ ] **[XLS 분리] 는 건드리지 않았다**
- [ ] `./validate.ps1 -All` 통과
