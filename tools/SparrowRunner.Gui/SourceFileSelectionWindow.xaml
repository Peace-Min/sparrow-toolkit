<Window x:Class="SparrowRunner.Gui.SourceFileSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="소스 파일 등록" Width="760" Height="640" MinWidth="560" MinHeight="420"
        WindowStartupLocation="CenterOwner" Background="#F6F8FA"
        FontFamily="Segoe UI, Malgun Gothic" FontSize="13">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="소스 파일 등록" FontSize="20" FontWeight="SemiBold" Foreground="#191F28"/>
        <Grid Grid.Row="1" Margin="0,8,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="SummaryText" VerticalAlignment="Center" Foreground="#4E5968" TextWrapping="Wrap"/>
            <Button Grid.Column="1" Content="전체 선택" MinWidth="82" Margin="8,0,0,0" Click="SelectAll_Click"/>
            <Button Grid.Column="2" Content="전체 해제" MinWidth="82" Margin="8,0,0,0" Click="ClearAll_Click"/>
        </Grid>
        <Border Grid.Row="2" Background="White" BorderBrush="#D1D6DB" BorderThickness="1" CornerRadius="8" Padding="8">
            <ListBox x:Name="FileList" ItemsSource="{Binding Choices}" BorderThickness="0"
                     VirtualizingPanel.IsVirtualizing="True" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Content="{Binding DisplayPath}" Margin="6,5"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="지원 형식: .c, .cpp, .cs, .h, .hpp" VerticalAlignment="Center" Foreground="#8B95A1"/>
            <Button Grid.Column="1" Content="등록" MinWidth="88" Margin="0,0,8,0" IsDefault="True" Click="Register_Click"/>
            <Button Grid.Column="2" Content="취소" MinWidth="88" IsCancel="True"/>
        </Grid>
    </Grid>
</Window>
